package com.github.altrunner.calculator;

public class ExpressionEvaluator {

    /**
     * I will evaluate the provided arithmetic expression and return the result.
     * For now I support only addition and subtraction of integer numbers but if Julia will not be lazy I might be able to do more.
     *
     * @return result of expression evaluation
     */
    public int evaluate(String exp) {
        return 0;
    }
}
